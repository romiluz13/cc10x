# cc10x Handoff — 2026-06-17

Pick-up doc for a fresh session after compaction. Durable facts also in memory: `project_cc10x_revitalization.md`.

## Where things stand

- **cc10x is v11.0.0, merged + pushed to `origin/main`** (tip `aff9e3b`). Working tree clean.
- Two campaigns shipped, all validator-gated (harness_audit, workflow_replay_check fixtures=24, doc_consistency_check, worldclass_benchmark) — green at every commit; full live harness 4/4 PASS.
  - **Campaign 1** (branch `v11-deversion-revitalization`, 10 commits): de-version state `.cc10x/v10/`→`.cc10x/` (version only in plugin.json/GitHub, fresh-start no migration); complexity gradient (trivial BUILD = reduced graph); copyable artifact skeleton + block-mode guard; Tier 1 quick wins; DG-2 router-supremacy softening; placeholder descriptions; CONFIDENCE-drift fix; leaked-phrasing purge.
  - **Campaign 2** (branch `v11-tier3-finish`, 5 commits): eval-driven authoring (`docs/EVAL-STANDARD.md` + evals for TDD/VBC/router); router kernel slim (622→602, validation tables → reference); BUILD-DONE finishing phase + worktree isolation; fork-strip (TDD 475→241, VBC 461→388, brainstorming 442→422, debugging 469→451 — all DIVERGENCE-noted, all needles kept).
- **Only deferred item:** agent-boilerplate dedup (Memory-First/SKILL_HINTS blocks) — load-bearing `Read(.cc10x/)` calls + a planner harness needle; intentionally left.
- The plan doc `docs/2026-06-17-cc10x-revitalization-plan.md` is an uncommitted working artifact (so is this file).

## NEXT CAMPAIGN — upstream idea harvest (NOT started)

**Trigger:** superpowers + matt-pocock shipped upstream updates. Goal = steal IDEAS, not re-fork.

**Key facts to act on:**
- Local superpowers cache `6.0.2` is **byte-identical to `6.0.0`** (`~/.claude/plugins/cache/claude-plugins-official/superpowers/`). The real update is **upstream on GitHub** — must fetch (WebFetch or octocode `obra/superpowers` or the official plugins repo) to see it. 14-skill roster as of 6.0.2: brainstorming, dispatching-parallel-agents, executing-plans, finishing-a-development-branch, receiving-code-review, requesting-code-review, subagent-driven-development, systematic-debugging, test-driven-development, using-git-worktrees, using-superpowers, verification-before-completion, writing-plans, writing-skills.
- matt-pocock skills **updated locally today 21:02** at `~/.agents/skills/` (installer: `setup-matt-pocock-skills`). Readable now — diff against memory of prior versions.

**ROM'S DECISIONS for the harvest (made 2026-06-17):**
- **Adoption bar:** RANKED LIST — agents produce a ranked steal-list, Rom approves each idea before any code. Nothing adopted blindly.
- **Sources:** (1) superpowers 6.0.2 — read locally, BUT note its on-disk content == 6.0.0 (cache identical), so the real new ideas are likely upstream on GitHub (`obra/superpowers` or official plugins repo) — fetch and cross-check there. (2) matt-pocock local skills at `~/.agents/skills/` (re-pulled 2026-06-17 21:02). NO broader ecosystem scan.

**Recommended harvest method (idea-first, validator-gated, like the last 2 campaigns):**
1. Fetch real upstream superpowers (GitHub) + read updated `~/.agents/skills/*` (matt-pocock).
2. Fan-out read-only agents to diff vs cc10x's current 14 skills + 10 agents → produce a RANKED list of *adoptable ideas* (new skill concepts, sharper techniques, new framings), each tagged: net-new skill vs delta-layer addition vs reject.
3. Rom picks. Then small implement passes, one validator-gated commit each, DIVERGENCE notes updated.
4. The fork-strip already did the hard part: forked skills are thin deltas now, so adopting an upstream refinement = edit the delta, not re-merge.

**Discipline reminders (hard-won this session):**
- Trust ONLY real loadable content (SKILL.md / agent .md / .py / fixtures / plugin.json). README/docs/bible are drifted — inventory-to-fix, never evidence.
- Even "read-only" workflow design-agents left edits in the working tree — ALWAYS `git status` + review every diff before committing agent output.
- 4 validators must stay green at every commit; full needle inventory lives in `cc10x_harness_audit.py` (prompt_phrase_guards, required_router_inline/surface, REQUIRED_FIXTURES). grep them before deleting/rewording any string.
- Branch off main, fast-forward merge, push — no force-push, no history rewrite.
